export interface LossesData {
  L_NAME: string;             // [L_NAME]
  SHIFT_NAME: string;         // [SHIFT_NAME]
  EV_T: string;               // [EV_T]
  EV_E: string;               // [EV_E]
  O_NM: string;               // [O_NM]
  R_NM: string;               // [R_NM]
  S_NM: string;               // [S_NM]
  COMM: string;               // [COMM]
  S_TIM: number;                // [S_TIM]
  PROD_YMD: Date;             // [PROD_YMD]
  SHIFT_CD: number;           // [SHIFT_CD]
  S_TIM_S: number;              // [S_TIM_S]
  EVENT_DATE: Date;           // [EVENT_DATE]
  PRG_NO: string;             // [PRG_NO]
  STEP_NO: string;            // [STEP_NO]
  LINE_CD: string;            // [LINE_CD]
  RESP: string;               // [RESP]
  CD: number;                 // [CD]
  DESC_FALLA: string;         // [DESC_FALLA]
  STATUS_L: string;           // [STATUS_L]
  FAILURE_CD: string;         // [FAILURE_CD]
  AFFECTATION_CD: string;     // [AFFECTATION_CD]
  SIX_CD: string;             // [SIX_CD]
  MACHINE_CD: string;         // [MACHINE_CD]
  MACHINE_DESC: string;       // [MACHINE_DESC]
  SIX_DESC: string;           // [SIX_DESC]
  AFFECTATION_DESC: string;   // [AFFECTATION_DESC]
  RESPONSABLE: string;        // [RESPONSABLE]
  TYPE_LOSSES: string;        // [TYPE_LOSSES]
}
